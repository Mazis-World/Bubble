{
  "buildCommand": "cd pwa && npm install --legacy-peer-deps --no-audit && npm run build",
  "outputDirectory": "pwa/build",
  "installCommand": "cd pwa && npm install --legacy-peer-deps --no-audit"
}
